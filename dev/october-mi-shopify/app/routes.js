import { flatRoutes } from "@remix-run/fs-routes";

// Use file-system flat routes from app/routes/*. This matches the current
// Shopify Remix app template (v3_routeConfig).
export default flatRoutes();
