/*
 * Placeholder for a local QR-code library (e.g. davidshimjs/qrcodejs or
 * node-qrcode browser build), bundled here to satisfy §12's "no external CDN
 * for core functionality" rule.
 *
 * Drop the minified library contents into this file. Until then, the ticket
 * template (frontend/templates/ticket.php) detects the missing `window.QRCode`
 * global and falls back to printing the ticket number, so check-in still works
 * via the ticket URL token.
 */
