/*
 * Placeholder for the html5-qrcode library (https://github.com/mebjas/html5-qrcode),
 * bundled locally to satisfy the "no external CDN for core" rule.
 *
 * Drop the minified library here (it defines window.Html5Qrcode). Until then the
 * check-in PWA detects the missing global and falls back to manual token entry,
 * so door check-in still works without the camera scanner.
 */
