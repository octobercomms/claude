{
    "name": "october/october-events",
    "description": "Atlanta Design Festival operations platform — accounts, listings, Stripe payments, Brevo email, ticketing and the AI Stories editorial connector.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.4",
        "stripe/stripe-php": "^13.0"
    },
    "comment-autoload": "Plugin classes load via the bespoke spl_autoload in october-events.php (sub-namespaces map to admin/, frontend/, migration/). Composer autoloading here exists only for third-party SDKs such as stripe/stripe-php.",
    "config": {
        "optimize-autoloader": true
    }
}
